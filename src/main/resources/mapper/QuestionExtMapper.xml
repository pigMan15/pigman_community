<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pigman.community.mapper.QuestionExtMapper">
  <resultMap id="BaseResultMap" type="com.pigman.community.domain.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Wed Jul 31 00:51:25 CST 2019.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="title" jdbcType="VARCHAR" property="title" />
    <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
    <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
    <result column="creator" jdbcType="INTEGER" property="creator" />
    <result column="comment_count" jdbcType="INTEGER" property="commentCount" />
    <result column="view_count" jdbcType="INTEGER" property="viewCount" />
    <result column="like_count" jdbcType="INTEGER" property="likeCount" />
    <result column="tag" jdbcType="VARCHAR" property="tag" />
  </resultMap>
  <resultMap extends="BaseResultMap" id="ResultMapWithBLOBs" type="com.pigman.community.domain.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Wed Jul 31 00:51:25 CST 2019.
    -->
    <result column="description" jdbcType="LONGVARCHAR" property="description" />
  </resultMap>

  <update id="incView" parameterType="com.pigman.community.domain.Question">

    update question
    set
      view_count = view_count + #{viewCount,jdbcType=INTEGER}
    where id = #{id}
  </update>

  <update id="incComment" parameterType="com.pigman.community.domain.Question">

    update question
    set
      comment_count = comment_count + #{commentCount,jdbcType=INTEGER}
    where id = #{id}
  </update>

  <select id="selectReleated" parameterType="com.pigman.community.domain.Question" resultMap="BaseResultMap">
    select * from question where id != #{id} and tag regexp #{tag}
  </select>

  <select id="countBysearch" parameterType="com.pigman.community.dto.QuestionQueryDTO" resultType="java.lang.Integer">
    select count(*) from question
    <where>
        <if test="search != 'null' and search != '' and search != null ">
            and  title regexp #{search}
        </if>
        <if test="mykey == 'zero' ">
        and comment_count = 0
        </if>
    </where>

  </select>

  <select id="selectBySearch" parameterType="com.pigman.community.dto.QuestionQueryDTO" resultMap="BaseResultMap">
    select * from question
    <where>
      <if test="search != 'null' and search != '' and search != null ">
        and  title regexp #{search}
      </if>
      <if test="mykey == 'zero' ">
        and comment_count = 0 order by gmt_create desc limit #{page},#{size}
      </if>
    </where>
    <if  test=" mykey == 'hot' ">
      order by  comment_count  desc, view_count desc  limit #{page},#{size}
    </if>
    <if test=" mykey == 'latest' ">
      order by gmt_create desc limit #{page},#{size}
    </if>

  </select>

</mapper>